{
  "name": "gifbox-api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node dist/app.js",
    "build": "tsc -p tsconfig.json --pretty",
    "dev": "tsc -p tsconfig.json --pretty --watch"
  },
  "type": "module",
  "devDependencies": {
    "@types/node": "^16.11.12",
    "typescript": "^4.5.3"
  }
}
